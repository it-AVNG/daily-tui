[project]
name = "daily-tui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.10"
dependencies = [
    "pytest>=9.0.2",
    "python-dotenv>=1.2.2",
    "sqlmodel>=0.0.37",
    "textual[syntax]>=8.1.1",
]

[dependency-groups]
dev = [
    "textual-dev>=1.8.0",
]
